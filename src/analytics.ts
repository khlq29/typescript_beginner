console.log(`sending .... `);
console.log("hi TSC");
console.log('yukinon');

// tsc -init
//tsc -w


// /tsc init will create a tsconfig.json file  
// it will now compile any .ts code if we type tsc in console
// tsc -w  can now watch all th codes with .ts extensions for any changes